- Favors composable, middleware-style architecture over simple paired hooks (e.g., replacing onBefore/onAfter with a chain of logging/caching/archiving/rate-limit middleware that each tool can opt into) — prioritizes designs that make adding new tools/features faster. Confidence: 0.4
- Prefers tools/features that fuse middleware (middleman/relay) mediation with durable memory persistence — values architectural components that simultaneously bridge request interception/relay with memory storage and retrieval (e.g., oracle_relay archiving the Q&A into the memory bank so the same question is answered from memory on future calls). Confidence: 0.8
- Prefers freshness-aware caching that invalidates cache entries using BOTH a recency window (e.g., N-hour expiry) AND input/resource-stability (e.g., file contents unchanged), not a single criterion — motivated by avoiding redundant backend calls to reduce cost and latency. Confidence: 0.7
# Workflow Preferences

- Uses "commit push" as a single shorthand command to stage, commit, and push all changed files in one go; expects the assistant to handle the full git workflow (status check, staging, commit message generation, rebase on divergence, push) without further instructions. Confidence: 0.8
- Comfortable with conventional commit format (`feat:`, `fix:` prefix, summary line, detailed bullet points, co-authored-by trailer). Confidence: 0.7
- Expects comprehensive documentation updates: when making changes, all relevant doc files (CHANGELOG, feature docs, README) should be updated together, not just the subset initially identified. Confidence: 0.6
- After initial documentation updates, expects a follow-up verification pass to confirm nothing was missed — systematically searches for stale version numbers or references across the full docs directory (e.g., via grep for old version patterns) rather than relying on memory alone. Confidence: 0.4
- Demands complete, production-quality implementation ("ทำเลยให้จบหมดเรียบร้อย version เต็ม") — rejects stubs, placeholders, or partial work; all pieces must be fully wired and functional. Confidence: 0.7
- Organizes work into clear priority tiers: "do now" / "do separately (theme-independent)" / "defer to next version" — prefers structured roadmaps with explicit grouping. Confidence: 0.6
- Writes detailed implementation plans (e.g., PLAN.md) with structured sections — Problem, Design, Target layout, Interfaces, tool-specific inputs, Execution steps, Out of scope, Acceptance criteria — and expects the assistant to read the plan and execute it step-by-step rather than improvising. Confidence: 0.8
- Direct, terse instructions override documented plan constraints (including the user's own "Out of scope" sections in PLAN.md) — when the user says e.g. "เริ่มทั้งสองงานเลย" (start both tasks now), the current intent takes precedence over prior written scope boundaries and the assistant should execute the redirected scope rather than refusing on plan grounds. Confidence: 0.6
- Treats the existing test suite as the contract for refactors: all existing tests must pass with zero test modifications as proof that behavior didn't change; bug fixes / new behavior get separate new regression tests instead. Confidence: 0.8
- Prefers refactors structured as additive, independently-shippable steps (e.g., steps 1–3 additive and landable separately) rather than one large risky change. Confidence: 0.6
- Considers a task unfinished until the work is actually installed/deployed for real use (e.g., "ติดตั้งใช้งานจริง" = install it for real) — expects the assistant to autonomously carry the completed code through build/install/deploy into the live environment, not stop at "code done and committed". Confidence: 0.6
- When asked to install/verify for real use (e.g., "ติดตั้งใช้งานจริงดูว่าปกติไหม", "ลองใช้ทุกอย่างเลยแล้วดูว่าแกติไหม"), expects a genuine end-to-end verification against the built artifact through the real interface/protocol — e.g., spawning the built MCP server and walking initialize → tools/list → tools/call over stdio — exercising ALL registered tools/features one by one, not just a representative sample, and not just reporting that build and unit tests are green. Confidence: 0.8
- Prefers verification failures to be triaged honestly into "real bug" vs. "bad test input" vs. "environment limitation" (missing API keys, unsupported backend config) — and expects the assistant to re-test with corrected input to confirm, rather than reporting raw FAILs as defects or dismissing them without follow-up. Confidence: 0.8
- Expects the assistant to debug its own verification harness (shell quoting, TS-in-.mjs syntax, JSON-RPC notification vs. request semantics) autonomously — iterating, writing temp scripts, and cleaning them up afterward — without asking the user for help. Confidence: 0.7
- Favors composable, middleware-style architecture over simple paired hooks (e.g., replacing onBefore/onAfter with a chain of logging/caching/archiving/rate-limit middleware that each tool can opt into) — prioritizes designs that make adding new tools/features faster. Confidence: 0.4
